import Ajv from 'ajv';
//import moduleSchema from './../schemas/module_schemas';


//const configureValidator = schema => (new Ajv({allErrors: true})).compile(schema);

// TODO: autogenerate this
/*export default {
  module: {
    test: configureValidator(moduleSchema.test),
  }
};
*/
